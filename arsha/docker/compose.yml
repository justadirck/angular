version: "3.8"

services:

  angular:
    container_name: angular-arsha
    image: justadirck/angular-arsha:latest
    build:
      context: ..
      dockerfile: ./docker/dockerfile
    ports:
      - 80:80
