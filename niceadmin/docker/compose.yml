version: "3.8"

services:

  angular:
    container_name: angular-niceadmin
    image: justadirck/angular-niceadmin:latest
    build:
      context: ..
      dockerfile: ./docker/dockerfile
    ports:
      - 80:80
