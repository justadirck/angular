version: "3.8"

services:

  angular:
    container_name: angular-two
    image: justadirck/angular-two:latest
    build:
      context: ..
      dockerfile: ./docker/dockerfile
    ports:
      - 80:80
