version: "3.8"

services:

  angular:
    container_name: angular-herobiz
    image: justadirck/angular-herobiz:latest
    build:
      context: ..
      dockerfile: ./docker/dockerfile
    ports:
      - 80:80
