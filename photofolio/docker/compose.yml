version: "3.8"

services:

  angular:
    container_name: angular-photofolio
    image: justadirck/angular-photofolio:latest
    build:
      context: ..
      dockerfile: ./docker/dockerfile
    ports:
      - 80:80
