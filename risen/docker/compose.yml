version: "3.8"

services:

  angular:
    container_name: angular-risen
    image: justadirck/angular-risen:latest
    build:
      context: ..
      dockerfile: ./docker/dockerfile
    ports:
      - 80:80
