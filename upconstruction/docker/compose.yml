version: "3.8"

services:

  angular:
    container_name: angular-upconstruction
    image: justadirck/angular-upconstruction:latest
    build:
      context: ..
      dockerfile: ./docker/dockerfile
    ports:
      - 80:80
