version: "3.8"

services:

  angular:
    container_name: angular-gp
    image: justadirck/angular-gp:latest
    build:
      context: ..
      dockerfile: ./docker/dockerfile
    ports:
      - 80:80
