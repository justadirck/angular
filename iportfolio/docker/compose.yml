version: "3.8"

services:

  angular:
    container_name: angular-iportfolio
    image: justadirck/angular-iportfolio:latest
    build:
      context: ..
      dockerfile: ./docker/dockerfile
    ports:
      - 80:80
