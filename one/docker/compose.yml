version: "3.8"

services:

  angular:
    container_name: angular-one
    image: justadirck/angular-one:latest
    build:
      context: ..
      dockerfile: ./docker/dockerfile
    ports:
      - 80:80
