version: "3.8"

services:

  angular:
    container_name: angular-yummy
    image: justadirck/angular-yummy:latest
    build:
      context: ..
      dockerfile: ./docker/dockerfile
    ports:
      - 80:80
